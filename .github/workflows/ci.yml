name: CI/CD Pipeline

# Note: frontend tests using jsdom should please start their files with
#  /**
#   * @jest-environment jsdom
#  */ 

on:
  push:
    branches:
      - main
      - dev
      - research
      - feat/**
      - docs/**
      - fix/**
      - test/**

  pull_request:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint

      # Run Jest in CI mode
      - name: Run tests
        run: npm test -- --ci

      - name: Build project
        run: npm run build

      # Upload build artifacts for deployment
      - name: Upload Pages artifact
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: test

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4